  ### ====================================================================================================
  ### [Local]
  ### ====================================================================================================
    
    if [ -d ~/bash_env/profile.d ]; then
      for file in $(echo ~/profile.d/*.sh); do
        [ -e "${file}" ] && . "${file}"
      done
      unset file
    fi
    
    
    
